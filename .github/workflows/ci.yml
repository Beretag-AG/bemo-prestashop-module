name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: ['7.2', '8.1']
    steps:
      - uses: actions/checkout@v7
      - uses: shivammathur/setup-php@v2
        # The token lets setup-php configure composer's GitHub auth, so dist
        # downloads are not throttled by codeload's anonymous rate limit.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          php-version: ${{ matrix.php }}
          coverage: none
      - run: composer install --no-interaction --prefer-dist
      - run: composer test

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: bash tests/package-structure.sh
